[package]
name = "{{app_name}}-desktop"
version = "0.1.0"
authors = ["Brainium Studios LLC"]
edition = "2018"

[dependencies]

[target.'cfg(target_os = "macos")'.dependencies]
{{app_name}} = { path = "../{{app_name}}", features = ["metal"] }
